# Azure Pipeline Variables Template
# Create these as Variable Groups in Azure DevOps:

# Variable Group: "ECR-Config"
variables:
- group: ECR-Config
  AWS_REGION: "af-south-1"
  AWS_ACCOUNT_ID: "573185292006"
  ECR_REPOSITORY: "cloudforge/cloudforge"

# Runtime Variables (can be overridden when manually triggering pipeline):
# IMAGE_TAG: Custom tag for Docker image (defaults to Build.BuildNumber)

# Service Connection Required:
# Name: AWS-Service-Connection
# Type: AWS
# Configure with IAM user credentials that have ECR permissions:
# - ecr:GetAuthorizationToken
# - ecr:BatchCheckLayerAvailability
# - ecr:GetDownloadUrlForLayer
# - ecr:BatchGetImage
# - ecr:DescribeRepositories
# - ecr:CreateRepository
# - ecr:InitiateLayerUpload
# - ecr:UploadLayerPart
# - ecr:CompleteLayerUpload
# - ecr:PutImage